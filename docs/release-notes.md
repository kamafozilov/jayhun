Download the package for your operating system from the release assets.

- Answer Codex questions from the composer.
- Keep closed files closed after workspace restoration, including when chat updates arrive during a pending save.
- Hide repeated Codex skills budget notices from new and saved conversations.
- Keep the sidebar at least 280 pixels wide.
- Start the development app with `npm start`. On Linux, the launcher applies a WebKit rendering workaround and preserves explicit overrides. Packaged builds are unaffected by this launcher change.

These personal builds use Tauri update signatures. macOS builds are not Apple notarized, and Windows installers do not have an Authenticode certificate.
